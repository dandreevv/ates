{
  "$schema": "http://json-schema.org/draft-04/schema#",

  "title": "Accounts.v1",
  "description": "json schema for account CUD event (version 1)",

  "definitions": {
    "event_data": {
      "type": "object",
      "properties": {
        "email": {
          "type": "string"
        },
        "public_id": {
          "type": "string"
        },
        "role": {
          "type": "string"
        }
      },
      "required": [
        "email",
        "public_id",
        "role"
      ]
    }
  },

  "type": "object",

  "properties": {
    "event_id":      { "type": "string" },
    "event_version": { "enum": [1] },
    "event_time":    { "type": "string" },
    "producer":      { "type": "string" },

    "data": { "$ref": "#/definitions/event_data" }
  },

  "required": [
    "event_id",
    "event_version",
    "event_time",
    "producer",
    "data"
  ]
}
