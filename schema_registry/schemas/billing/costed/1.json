{
  "$schema": "http://json-schema.org/draft-04/schema#",

  "title": "Tasks.v1",
  "description": "json schema for billing task event (version 1)",

  "definitions": {
    "event_data": {
      "type": "object",
      "properties": {
        "public_id": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "assigner_id": {
          "type": "string"
        },
        "status": {
          "type": "string"
        },
        "created_by": {
          "type": "string"
        }
      },
      "required": [
        "public_id",
        "status",
        "fee",
        "cost"
      ]
    }
  },

  "type": "object",

  "properties": {
    "event_id":      { "type": "string" },
    "event_version": { "enum": [1] },
    "event_time":    { "type": "string" },
    "producer":      { "type": "string" },

    "data": { "$ref": "#/definitions/event_data" }
  },

  "required": [
    "event_id",
    "event_version",
    "event_time",
    "producer",
    "data"
  ]
}
